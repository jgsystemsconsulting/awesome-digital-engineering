# Spec: pin-validate-setup-python

## Goal
SHA-pin mutable GitHub Actions tags across validate and link-check workflows.

## Research
research: skipped (pin SHAs from known action tags / sibling gold sample)

## Requirements
- Pin checkout, setup-python, setup-node, create-issue-from-file to full commit SHAs with version comments
- Leave lychee-action pin unchanged
